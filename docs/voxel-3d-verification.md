# 体素 3D 改造验证记录

日期:2026-09-23
执行方式:会话内逐 Task 实现(原子代理派发因 API 余额耗尽不可用),每个 Task 结束跑 `pnpm verify`,渲染类 Task 另在真实浏览器实测。浏览器实测由执行方驱动,基于 `pnpm dev` 起的开发实例(3201 端口)。

## 一、自动化

| 检查 | 命令 | 结果 |
|---|---|---|
| lint + typecheck + test + build | `pnpm verify` | **EXIT=0**;lint `16 problems (0 errors, 16 warnings)`(与改造前基线一致)、3 项 typecheck 全过、build 全过 |
| web 测试 | `pnpm --filter @ai-town/web exec vitest run` | **67 passed**(改造前 12 → 新增 55 条) |
| server 测试 | — | **137 passed**(未受影响) |

新增单测覆盖的纯函数:`sceneCoords`(6)、`groundPlan`(5)、`voxelMath`(8)、`buildVoxelActor`(9)、`actorSpecs`(6)、`pathPlayback`(6)、`bubbleText`(4)、`sunFromGameMinute`(8)、`buildingPlan`(5)、`walkablePlan`(4)。

## 二、浏览器实测

在 `http://localhost:3201/world/world_qixi_town`(Chrome,视口 1266×671,DPR 2)逐项确认。

| Task | 结果 | 实测要点 |
|---|---|---|
| 3 场景骨架 | ✅ | 清空 localStorage 后默认 2D(当时默认值还是 2D);切 3D 后容器恰好 900×620;草地/河流/路面/广场全部渲染,方位与 2D 地图一致(河流偏左、西岸路在河左侧、桥横向跨河 → 无镜像);拖拽环绕生效;俯仰夹取在 `[0.12π, 0.42π]`,相机无法钻到地面以下;点河面返回「那里无法到达，请点击道路或广场」;点可走点是则成功规划并把玩家移动过去;切换偏好双向持久化且刷新后保留;切回 2D 后 Phaser 场景与改造前一致 |
| 6 体素演员 | ✅ | 5 名居民以体素体积站立在地面上,位置与 blueprint 一致;环绕一圈各个角度都成立(箱体几何,非 billboard);近距离可见唐雨澄的紫色头发;逐个点击居民均打开居民详情(沈知衡→「衡」、何建国→「国」;林夏/周放/唐雨澄 同在 (270,190),该点选中最近的那个);点击居民后玩家坐标不变 → `stopPropagation` 生效 |
| 7 路径回放 | ⚠️ 部分 | 居民沿服务端路径行走并最终停在其权威位置(实测沈知衡从诊所方向走过桥);点地面 → 服务端规划 → 玩家移动到位、事件流出现「你移动到了新的位置」。**未隔离验证「亚 tick 平滑插值」**:截图调用之间相隔数分钟,无法在单个 2s tick 内取两帧。该性质由代码保证(位置/朝向每帧写入场景对象,"只对齐一次"与逐帧接管分离),且数分钟观察未见 tick 间抖动 |
| 8 名牌气泡 | ✅ | 6 个名牌(5 居民 + 玩家「你」)带姓名与当前行动;气泡经 DOM 投影,中文不依赖字体图集;发起一次对话后玩家与居民头顶同时出现气泡;长回复(>72 字)气泡被截断带省略号,右侧面板保留全文 |
| 10 光照与阴影 | ✅ | 夜间(04:58)整体转暗蓝、居民仍有轮廓;黄昏/傍晚偏暖偏暗;正午(11:50)亮且色板正确(薄荷草地/米色路面/深青河面);**阴影可见**(正午短且贴近脚下,方位随太阳变化) |
| 11 体素建筑 | ✅ | 5 个建筑以 3D 体块立起、位置与 blueprint 对应,带屋顶压顶与窗;夜间(21:59)窗户发暖黄自发光,小镇成剪影 |
| 12 叠加层与水面 | ✅ | 开「行走区域」后青色方格**平铺**在可走区域(路面/广场/草地),不覆盖建筑;关掉即消失。水面在锁定调色板两色之间呼吸 |
| 13 小地图与默认值 | ✅ | 清空 localStorage 刷新 → 直接进 3D;左上角小地图显示 AI 生成的俯视图并带居民彩点;切 2D 时小地图不渲染 |
| 14 分包与降级 | ✅ | 入口 chunk 357.84 KB(109.43 KB gz)内**既无 Phaser 也无 three**;`TownCanvas` 1222.97 KB、`Town3DCanvas` 942.81 KB(253.42 KB gz)各自按需加载;缩到 400px 宽刷新后 lite 档仍正常渲染 |

### 功能对等核对(3D vs 2D)

| 能力 | 2D | 3D |
|---|---|---|
| 点地移动 | 改造前已验 | ✅ 实测(含河面/建筑被服务端拒绝) |
| 点居民选人 | 改造前已验 | ✅ 实测(逐个居民) |
| 对话气泡 | 改造前已验 | ✅ 实测(玩家 + 居民,72 字截断) |
| 暂停 / 继续 | 改造前已验 | ✅ 实测(验证期间反复使用) |
| 跳过时间 | 改造前已验 | ✅ 实测(+3 时多次,光照随时钟变化) |
| 创建分支 | 改造前已验 | ⚠️ **未在 3D 下单独复验**。该按钮不依赖渲染器,但本次没有点过 |
| 行走区域高亮 | 改造前已验 | ✅ 实测 |

## 三、实测参数(最终值)

- 相机:默认位置 `[cx, 680, cz + 800]`(距目标 ≈1050,极角 ≈49.6°);俯仰夹取 `[0.12π, 0.42π]`(21.6°–75.6°);缩放 `[140, 1700]`
- 体素:`VOXEL_SIZE = 1`(= 1 world px);角色 10 宽 × 6 深 × 28 高
- 地面:草地基座顶面 `y = 0`;水面 `y ∈ [-0.075, +0.175]`;路面/广场 `y ∈ [+0.055, +0.305]`;可走叠加层 `y = 1.4`
- 建筑:高度 `min(60, 24 + 宽度/10)`;屋顶外扩 8
- 太阳:日出 06:00 / 日落 18:00,高度角正弦 ±70°;夜间环境光 0.6、方向光 0.3
- 分包(最终 build 实测):入口 `index-*.js` 357.84 KB(109.43 KB gz)、three chunk `Town3DCanvas-*.js` 942.81 KB(253.42 KB gz)、Phaser chunk `TownCanvas-*.js` 1222.97 KB(335.26 KB gz)。入口内既无 Phaser 也无 three
- 单帧 draw call 峰值:**未直接测量**。结构上每角色 6 个 `InstancedMesh`(1 draw call/部件)、地面 1 基座 + 9 quad、5 个建筑各约 (墙体+屋顶+门+窗)≈4–8 个、叠加层 1 个
- 帧率:**未用专业工具测量**。浏览器拖拽/缩放交互流畅,未观察到卡顿

## 四、未做 / 已知限制

- **无物理与 3D 碰撞**(设计如此):参考系仍是平面,碰撞与寻路由服务端导航网格负责,3D 只做表现。
- **河面是贴地贴片而非下陷河床**:草地基座是不透明实体且覆盖全图,要让河真正下陷需要基座挖掉河流footprint,不在本期范围。
- **平移无边界、无「回到中心」**:Plan 的 §12 列为待调项,本期未做。
- **2D 精灵表相关代码保留未清**:`chromaKeySheet` / `registerSpriteSheets` / `WALK_FRONT_FRAMES` 仍是 2D 回退路径的依赖,按设计等回退退役时一并清理;`game3d/ui/bubbleText.ts` 与 `TownScene.ts` 的 `clipBubbleText` 目前是刻意重复(为守住"不改 2D 渲染器")。
- **反复切换渲染器后出现过 `THREE.WebGLRenderer: Context Lost`**:在一次会话里多次 2D↔3D 切换 + 大量缩放/环绕后出现,单次切换不可复现。建议后续确认卸载时 three 的上下文是否被释放。
- **亚 tick 插值未隔离验证**(见 Task 7 行)。
- **创建分支按钮未在 3D 下复验**。

## 五、审查提出但本次未修的发现

逐条记录,附判断依据(供后续决定是否处理):

| 发现 | 判断 |
|---|---|
| `.town-canvas-3d` 用 `!important` 压 R3F 的行内 `width/height: 100%` | 可用。审查建议改在 `<Canvas style={{…}}>` 上传(据 R3F 实现,`...style` 在其默认值之后展开,无需 `!important`),更干净;非阻塞 |
| `.town-canvas-3d canvas {…!important}` 是多余的(该 canvas 没有行内尺寸) | 冗余样式,无功能影响 |
| `groundPlan.test.ts` 用魔数 `width === 54` 定位桥面 | 当前 54 在四条路径里唯一,但 blueprint 改动复用该宽度时会静默断言到别的路段 |
| `GroundMesh.tsx` 注释枚举了 6 个面相异平面中的 5 个(漏了基座顶面 y=0) | 纯注释,结论仍成立 |
| `sceneToWorld` 对负半值的取整(`Math.round(-0.5) === -0`)未被测试钉住 | 对 JSON 安全(`JSON.stringify(-0) === "0"`),整数契约未被测试覆盖 |
| `worldCenter` 不做取整 | 作为相机 target 无害;将来做平移夹取时不能把它当整数世界坐标用 |
| 切换开关显示的是**当前**渲染器而非目标动作(在 2D 时显示「2D 视图」) | 计划原文如此,读起来像状态标签而非按钮 |
| 反复切换渲染器后出现过 WebGL 上下文丢失 | 单次切换不可复现;建议确认卸载时 three 是否释放了上下文 |
| `VoxelPartMesh` 依赖 `ActorBody` 对 `buildVoxelActor(spec)` 的 memo | 当前 spec 来自模块常量,只是潜在风险 |
| `game3d/ui/bubbleText.ts` 与 `TownScene.ts` 的 `clipBubbleText` 重复 | 刻意为之(守住"不改 2D 渲染器"),等回退退役时去重 |

已修:`sceneCoords.test.ts` 那条测试原名声称"回退成 +z 方向",但实现里并无回退分支 —— 改为按真实机制命名,并用 `toBeCloseTo` 避开 `-0` 的 `Object.is` 陷阱。
