# 待完成需求文档 · AI 蝴蝶小镇

> 基线：`docs/living-requirement-map.html`（Living Requirement Map v1.5，D01–D132）。
> 生成方式：逐条对 HTML 的需求（编号 / 决策 / 落地）与**当前代码与测试**交叉比对。
> 状态标记：✅ 已实现　🔶 部分（已落地但缺关键闭环）　⬜ 未做
> 原则：只列真正影响「面试主流程 / 交付可验收」的剩余项；D125–D132 为路线图/参考边界，不重复为需求。
> ⭐ **用户强调的重点**：NPC 记忆系统（D13/D14）必须做扎实，是项目重点——短期事件 + 长期摘要 + 关系印象，按近期/重要/相关检索，重要经历积累形成反思；不做只写不读的占位，要真正影响后续行为。

## 一、已实现（仅快速浏览，不展开）

D01–D12 世界骨架与 NPC 自主（5 NPC、连续实时、暂停/继续、Mock+真实 AI 决策、每 Tick 预算、受控并发·部分）
· D37 时间比例（2s=1 小镇分钟）
· D39/D40 状态 0–100 数值与核心字段（hunger/energy/mood/stress/social）
· D47/D48/D50/D51 AI+规则：候选受限、结构化输出校验修复一次、失败降级、AI Trace 面板
· D18–D22 玩家：居民实体、全局观察、A* 移动（含逐格行走/跨桥/绕行）、自动接近、对话+3 快捷动作（部分）
· D26 实时对话参与者锁（**缺紧急中断**）
· D41 多维关系（familiarity/trust/liking/respect/labels/summary 已写入）
· D61/D62/D67/D68/D69 持久化+Mock：事件日志+初始快照+刷新恢复、无 Key 完整体验、Mock 决策/对话
· D73–D90 架构大部分（TS Monorepo、Fastify、SQLite、REST+WS、Zod/Drizzle、网格 A*、账户登录、日志）
· D82 地图寻路（Blueprint 权威 + 网格 A*；NPC 沿 A* 路径逐格行走，「行走区域」叠加层）
· D99 CI（**无 lint**）、D101 README/技术方案、D108 提交历史
· D112–D121 内置栖溪镇 + 5 NPC + 场景（咖啡馆/诊所重点室内；Seedream 5.0 精灵表生成脚本 `pnpm generate:sprites`）
· D124 内容边界（无固定剧情）
· M7 生成管线**内部**：结构生成/校验/模板/寻路 + 6 单测（`world-generator`/`world-structure`，**未接线**）

## 二、已完成 · 主线功能(P0/P1,2026-08-28 夜完成)

> 以下各项均已实现并由 `pnpm test:e2e`(10 步)与浏览器全流程验证;原「差」描述保留作为实现记录。


### M5 事件注入与因果链（A；D 已做领域层 `event-preview.ts`，含天气类模板）
- ⬜ **D23 事件注入** 模板+自由文本→AI 解析→结构化预览→确认才写世界。差：`preview`/`commit` 路由、`WorldPage` 事件侧栏、参数化模板。
- ⬜ **D24 信息可见性** 按事件类型传播；区分客观事实 / 居民可知 / 主观理解。差：Knowledge 写作与可见性计算。
- ⬜ **D25 / D97 蝴蝶效应** 父事件+决策依据的因果链，≥1 级后续，可反向追踪。差：`causeIds` 连通、事件触发重规划。
- ⬜ **D55 事件与因果** 紧凑事件流 + 大型分析视图（时间线、因果图、节点详情）+ 按居民/地点/类型/来源过滤。差：`CausalPage`。
- ⬜ **D57 事件入口** 观察者工具栏打开事件侧栏，确认前不得改世界。差：前端入口。
- 🔶 **D122 演示事件**「气象台发布暴雨预警，河岸场地可能封闭」（08:40 注入）。领域层模板已有 `weather_alert` 词表，但**无注入 + 传播端到端**。

### M6 快照 / 分支 / 跳过
- ⬜ **D38 跳过时间** 跳到当前动作结束/下一重要事件，后台完整模拟，紧急事件提前停止。差：`skip` 接口与前端。
- ⬜ **D64 时间线分支** 从历史节点继续→新建 `branch_id`，原时间线不变。差：分支操作接口（现仅默认分支+初始快照）。
- ⬜ **D66 存储清理** 滚动快照/里程碑/资产哈希去重/无引用清理。
- 🔶 **D96 面试持久化**：已有追加事件+初始快照+刷新恢复；差：周期/里程碑快照、可移植世界包导入导出。

### M7 动态世界生成（接线，内部逻辑已就绪）
- ⬜ **D53 创建页面** 一句话输入 + 高级选项（人口/风格/后果/审批/模式/种子）+ 分阶段真实进度 + 失败单独重试。差：`POST /worlds`、`/worlds/new` 路由与页面。
- ⬜ **D92 双轨**：内置镇已好；差：一句话真实生成结构/NPC/规则并入库（`WorldGenerator` → `jobs`/`worlds`）。
- ⬜ **D29/D30 动态生成与扩建** 结构/AI 美术/运行中扩建三层；扩建提案预览→确认→写入。
- 🔶 **D31/D32 地图生成与审查失败**：像素风格锁、美术脚本（Seedream）已有；差：应用内 IMAGE/VISION 真实 HTTP + 蓝图对比校验 + 超限程序化降级。
- ⬜ **D34 动态人口** 按规模/地点/职业建议人口，默认 5、目标 20。
- ⬜ **D35 能力标签校验** 用 `eat/rest/work/social/safety/health/public_info` 标签，保证每项核心需求有满足方式。
- ⬜ **D36 地图扩张** 优先填空地、必要时生成相邻新区、道路连通且不覆盖原有状态。
- ⬜ **D28/D94 室内深度** 主要建筑全部可进入；两个核心室内（咖啡馆/诊所）重点美术+丰富交互，其余程序化模板。

### M8 AI 调试工作台
- ⬜ **D98 调试工作台** 上下文/Prompt 检查、临时编辑、沙盒重放、AI vs Mock 或双模型对比；重放默认不写回。差：`/ai/replay`、`/ai/compare` 与对应页面（现有仅 `GET /agents/:id/decisions` 的 Trace 面板）。

## 三、待完成 · 非主线能力（P1 / Reserved，缺关键闭环）

- ⭐**重点** 🔶 **D13/D14 记忆**：仅对话写入 `memories` 表未检索；差：短期事件+长期摘要+关系印象、按近期/重要/相关检索、高重要经历形成反思（D14 预留 FTS5/Embedding 接口）。用户强调需做扎实，是项目重点，非占位。
- 🔶 **D15 规划层级** 长期目标→日计划→当前动作；差：仅当前动作，无目标/计划层。
- 🔶 **D22 玩家动作** 仅对话+3 快捷；差：复用 NPC 动作工具、复合指令仅预留接口。
- ⬜ **D26 紧急中断**（对话中紧急事件释放参与者锁）。
- ⬜ **D42 经济闭环** 工作收入/购买交换/真实库存/补货/消耗/转交；金钱与物品变化有来源去向（价格/配方仅预留字段）。
- ⬜ **D44 资源并发** 意图校验+到场短期预约+原子执行，防重复购买/负库存/多人占用。
- ⬜ **D45 极端后果** 强制行为与能力限制，可配置昏迷/失业/破产/死亡。
- 🔶 **D46/D84/D95 模型角色**：仅 `SIMULATION` 真实接入（决策+对话）；差：`WORLD`（结构生成）、`IMAGE`/`VISION`（应用内 HTTP）、按任务 Token 预算（D49）。
- ⬜ **D63 任务恢复** `jobs` 表 + 进程内 Worker（租约/重试/幂等/进度），进程重启可恢复。
- 🔶 **D59 设备范围** 桌面完整；平板/手机未验证，复杂工具未简化。
- 🔶 **D72 错误体验** 部分错误有说明/重试/降级；差：错误 ID/开发者详情、永久 Loading 兜底。

## 四、待完成 · 交付与工程化（M9）

- 🔶 **D100 核心路由** 现已 3 条（登录/首页/主界面）；差：`/worlds/new`（创建）、`/world/:worldId/causal`（因果）、`/dev/ai`（工作台）。
- ⬜ **D102 Docker** `Dockerfile`/compose 已写；阻塞：`packages/shared` 以 `exports: "./src/index.ts"` 指向源码 → `node dist` 解析 `.ts` 失败。需改为 `dist` 导出（跨切片，待统一裁定）。
- ⬜ **D103 线上 HTTPS** 公开地址+演示账号+有效期+冷启动提示。
- ⬜ **D104 演示视频** 约 5 分钟，覆盖自主行动/决策/对话/事件因果/Mock/恢复/工作台。
- ⬜ **D105 现场改码案例**「新增动作工具」全链路（Schema→合法校验→执行器→Mock→UI→测试）。
- ⬜ **D106 AI 修改案例** 记录 AI 初版物品转交的事务/幂等风险与人工修正过程。
- 🔶 **D107 决策账本** 已在 docs 有部分；补「AI 参与方式/个人取舍/验证/推翻」并引用代码与测试。
- ⬜ **D109 OpenAPI** 从 Fastify 路由 Schema 自动生成（现路由未挂 schema）。
- ⬜ **D110 线上 AI 额度** 演示账号按能力限真实调用，耗尽自动 Mock + 复用缓存。
- 🔶 **D111 交付检查** `pnpm verify` = typecheck+test+build（`delivery:check` 已加）；补：密钥/默认密钥检查、lint、空库迁移+Seed+登录+健康烟测。
- 🔶 **D70 测试范围** 有单元/API 集成；差：E2E 单条主流程冒烟、故障注入。
- ⬜ **D71 真实 AI 测试** 离线已全；差：手动/定时四类模型契约测试（`test:ai`）。

## 五、一句话结论（给接任/分工）

- **已可交付主轮廓**：登录→栖溪镇→5 NPC 自主→玩家移动/对话→刷新恢复→无 Key 完整体验 —— 且 39 项服务端单测全绿、`pnpm verify` 通过、CI 已配。
- **最优先未完成**（面试主流程）：M5 事件注入 + 因果链 / CausalPage（D23/D24/D25/D55/D57/D97/D122）→ 六路由补齐 3 条（D100）→ M7 一句话创建接线（D53/D92）。
- **交付钉子**：Docker 的 shared `src`→`dist` 导出（D102）、OpenAPI（D109）、线上地址+额度+视频（D103/D104/D110）、`delivery:check` 补 lint/种子烟测（D111）。
